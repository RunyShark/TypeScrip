"use strict";
(() => {
    var _a;
    //solo acepta numeros
    let pablito = "Pablito";
    console.log(((_a = pablito[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || "No hay datos suficientes");
})();
