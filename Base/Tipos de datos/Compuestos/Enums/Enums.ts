(() => {
  enum AudioLevel {
    min,
    medium,
    max,
  }

  let currenteAudio: AudioLevel = AudioLevel.min;
  let currenteAudio1: AudioLevel = AudioLevel.max;
  let currenteAudio2: AudioLevel = AudioLevel.medium;
})();
